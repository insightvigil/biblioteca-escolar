{
  "name": "server",
  "version": "1.0.0",
  "description": "This is the backend for the app",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node --watch src/app.js",
    "db:init": "psql -h $DB_HOST -U $DB_USER -d $DB_NAME -f db/001_init.sql"
  },
  "author": "Jose Adrian Flores Vigil",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^17.2.1",
    "express": "^5.1.0",
    "nodemon": "^3.1.10",
    "pg": "^8.16.3"
  }
}
